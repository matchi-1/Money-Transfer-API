server.name: kibana
server.host: "0.0.0.0"
elasticsearch.hosts: ["${ELASTICSEARCH_HOSTS:https://elasticsearch:9200}"]
elasticsearch.serviceAccountToken: ${ELASTICSEARCH_SERVICEACCOUNTTOKEN}
elasticsearch.ssl.verificationMode: none
